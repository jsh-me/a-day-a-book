<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aday.abook">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".feature.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".feature.search.BookSearchActivity"/>
        <activity android:name=".feature.memo.BookMemoActivity"
            android:theme="@style/Theme.Transparent" />
        <activity android:name=".feature.addbook.AddBookActivity"/>
        <activity android:name=".feature.setting.SettingActivity"/>
        <activity android:name=".feature.pwd.PwdActivity"/>
    </application>

</manifest>